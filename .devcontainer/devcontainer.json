{
  "name": "security-learning-lab",
  "dockerComposeFile": "../compose.yaml",
  "service": "app",
  "workspaceFolder": "/app",
  "updateRemoteUserUID": true,

  // コンテナ起動時のコマンド
  "postCreateCommand": "bun install",

  // Next.js dev server のポート
  "forwardPorts": [3000],

  // 🔽 node_modules と bun cache を永続化する設定
  "mounts": [
    "source=nextjs_node_modules,target=/workspaces/${localWorkspaceFolderBasename}/node_modules,type=volume",
    "source=bun_cache,target=/home/node/.bun,type=volume"
  ],
  "remoteUser": "root"
}
